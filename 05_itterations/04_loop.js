const myObject = {
  js: "javascript",
  cpp: "c++",
  rb: "ruby",
  swift: "swift byapple",
};

// for (const key in myObject) {
//   console.log(`${key} shortcut is for ${myObject[key]}`);
// }

const arr = [1, 2, 2, 2, 2, 2, 2, 2];

for (const key in arr) {
  console.log(`${key} vallue is ${arr[key]}`);
}
